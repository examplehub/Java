package com.examplehub.maths;

public class SumToN {

  /**
   * Sum from 1 to n.
   *
   * @param n the limitation.
   * @return sum of 1 to n.
   */
  public static int sum(int n) {
    int sum = ((n)(n+1))/2;
    return sum;
  }
}
